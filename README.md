# منصة مزادي - مشروع مزادات مباشرة متكامل

هذا المستودع يحتوي على الكود المصدري الكامل لمنصة "مزادي" للمزادات المباشرة عبر الإنترنت. المشروع مقسم إلى واجهة خلفية (Backend) وواجهة أمامية (Frontend) ويعمل بشكل متكامل باستخدام Docker.

## ✨ الميزات الرئيسية

- **الواجهة الخلفية (Backend):** مبنية باستخدام Node.js, Express, MongoDB, و Socket.IO.
- **الواجهة الأمامية (Frontend):** مبنية باستخدام React, Vite, و Tailwind CSS.
- **تحديثات فورية:** استخدام Socket.IO لتحديث أسعار المزادات والمزايدات بشكل لحظي.
- **نظام إشعارات:** إشعارات للمستخدمين عند المزايدة، الفوز، أو اقتراب انتهاء المزاد.
- **بحث وفلترة:** نظام بحث متقدم لفلترة المزادات حسب السعر، الفئة، أو الكلمات المفتاحية.
- **أمان:** حماية المسارات باستخدام JWT، تشفير كلمات المرور، وتحديد معدل الطلبات (Rate Limiting).
- **جاهز للنشر:** المشروع مجهز بالكامل للتشغيل في بيئة الإنتاج باستخدام Docker و Docker Compose.

---

## 🚀 التشغيل باستخدام Docker

الطريقة الأسهل والأسرع لتشغيل المشروع بالكامل هي باستخدام Docker Compose.

### المتطلبات

- [Docker](https://docs.docker.com/get-docker/)
- [Docker Compose](https://docs.docker.com/compose/install/)

### خطوات التشغيل

1.  **استنساخ المستودع:**
    ```bash
    git clone https://github.com/shxii9/new-mzad.git
    cd new-mzad
    ```

2.  **إعداد متغيرات البيئة:**
    انسخ ملف `.env.example` إلى ملف جديد باسم `.env` واملأ المتغيرات المطلوبة.
    ```bash
    cp .env.example .env
    ```
    افتح ملف `.env` وقم بتعبئة القيم التالية:
    ```env
    # مفتاح سري قوي لـ JWT
    JWT_SECRET=your_very_strong_jwt_secret_key

    # بيانات Cloudinary لرفع الصور
    CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
    CLOUDINARY_API_KEY=your_cloudinary_api_key
    CLOUDINARY_API_SECRET=your_cloudinary_api_secret
    ```

3.  **بناء وتشغيل الحاويات:**
    استخدم Docker Compose لبناء الصور وتشغيل جميع الخدمات (الواجهة الأمامية، الواجهة الخلفية، قاعدة البيانات).
    ```bash
    docker-compose up --build -d
    ```

4.  **الوصول إلى التطبيق:**
    - الواجهة الأمامية ستكون متاحة على: `http://localhost`
    - الواجهة الخلفية ستكون متاحة على: `http://localhost:5002`

---

## 🔧 التشغيل المحلي (بدون Docker)

يمكنك أيضًا تشغيل كل جزء من المشروع بشكل منفصل.

### الواجهة الخلفية (Backend)

- انتقل إلى مجلد `backend`.
- قم بتثبيت التبعيات: `npm install`.
- انسخ ملف `.env.example` إلى `.env` واملأ المتغيرات.
- قم بتشغيل الخادم: `npm run dev`.

### الواجهة الأمامية (Frontend)

- انتقل إلى مجلد `frontend`.
- قم بتثبيت التبعيات: `npm install`.
- انسخ ملف `.env.example` إلى `.env` واملأ المتغيرات.
- قم بتشغيل خادم التطوير: `npm run dev`.

---

## 📂 بنية المشروع

```
new-mzad/
├── backend/         # مشروع الواجهة الخلفية (Node.js/Express)
│   ├── Dockerfile
│   └── ...
├── frontend/        # مشروع الواجهة الأمامية (React/Vite)
│   ├── Dockerfile
│   └── ...
├── docker-compose.yml # ملف Docker Compose لتشغيل المشروع
├── .env.example     # مثال لملف متغيرات البيئة
└── README.md        # هذا الملف
```
